{
  "name": "nihongo-quest",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "powershell -NoProfile -Command \"New-Item -ItemType Directory -Force dist,dist/src,dist/assets | Out-Null; Copy-Item index.html dist/index.html -Force; Copy-Item src/*.js,src/*.css dist/src/ -Force; Copy-Item assets/*.webp dist/assets/ -Force\""
  }
}
