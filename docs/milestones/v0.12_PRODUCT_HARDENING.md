# v0.12 — Product Hardening

Status: focused save-reliability slice implemented and verified.

The canonical save boundary now tolerates malformed collection shapes during import, removes invalid review records, preserves the schema migration contract, and keeps in-memory progress updates working when browser storage is unavailable or quota-limited. No progression rules, save fields, or user-facing learning semantics changed.

Focused tests cover malformed legacy-compatible imports and storage-write failures. The full regression suite and production build pass. Remaining operational warnings are limited to intentionally untracked local audit/build artifacts; they are not part of the published runtime.
