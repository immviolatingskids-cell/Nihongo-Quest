# Nihongo Quest v0.9.7 — Living World Synthesis & v0.9 Closure

## Release-hardening slice

The v0.9 surfaces now share the Sakura Horizon token layer and a readable five-column mobile navigation grid. Key companion and shop artwork declares intrinsic dimensions and lazy decoding where it is below the fold, reducing layout shift and avoiding eager loading of non-primary artwork. Existing scene layers and cultural art retain their approved WebP runtime paths and text fallbacks.

Accessibility checks retain semantic buttons, meaningful image alternatives, polite Sakura status announcements, visible keyboard focus, text-only culture fallbacks, and reduced-motion rules. No progression, save, Garden, discovery, or ownership semantics changed.

## Audit record

- Full regression suite: passing.
- Production copy build: passing.
- JavaScript syntax and whitespace checks: passing.
- Desktop, tablet, and approximately 390px browser checks: no horizontal overflow; Home recommendation, Journey map, Sakura status region, and five-column mobile navigation verified.

## Non-blocking warnings

- The repository retains historical source PNGs alongside optimized WebP runtime derivatives by design; PNGs are the approved source handoff and are not referenced by runtime scene URLs.
- Existing generated archives, local browser profiles, and build scratch directories remain untracked workspace artifacts and are intentionally excluded from release commits.
- A future v1.0 pass may further consolidate legacy pre-Sakura CSS declarations; the active Sakura Horizon token layer is canonical for v0.9 surfaces.
